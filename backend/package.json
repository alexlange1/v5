{
  "name": "@gist/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Gist backend — a thin Cloudflare Worker that holds the model credentials, injects the brevity system prompt, and enforces the daily volume cap.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "@types/node": "^22",
    "typescript": "^5.6",
    "vitest": "^2.1",
    "wrangler": "^3.80"
  }
}
